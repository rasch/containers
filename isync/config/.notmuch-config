# user.name - is set using the NAME environment variable
# user.primary_email - is set using the EMAIL environment variable
# database.path - the mail directory (which contains the notmuch database)
#   is mounted in the default location "$HOME/mail" in the container

[new]
ignore=.mblaze;.mbsyncstate;.netrc.asc;.s-nailhist;.uidvalidity;dead.letter;
